name: Test Saagie Dev Tools runners

on:
  workflow_dispatch:
  push:
    branches:
    - test/actions-runner

jobs:
  "test-devtools-saagie:dev2":
    runs-on: "owner:saagie-devtools|env:dev2"
    steps:
    - name: "Hello World"
      uses: docker://docker/whalesay
  "test-devtools-saagie:prod":
    runs-on: "owner:saagie-devtools"
    steps:
    - name: "Hello World"
      uses: docker://docker/whalesay