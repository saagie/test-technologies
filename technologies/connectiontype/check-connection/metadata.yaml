version: v2
type: CONNECTION_TYPE
id: check-connection
label: Check Connection

parameters:
  - type: TEXT
    id: username
    label: Username
    mandatory: true
  - type: PASSWORD
    id: password
    label: Password
    mandatory: true

actions:
  check:
    script: ./dist/module.js
    function: checkConnection
